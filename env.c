#include <stdio.h>
#include <unistd.h>
#include "main.h"

/*Return: Always 0*/

int main(int argc, char *argv[], char *env[])
{
	int i;

	for (i = 0 ; env[i] ; i++)
std:: cout << env[i] << std::endl
std::cout << std::endl

	return (0);
}
